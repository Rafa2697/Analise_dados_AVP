{
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}/dashboards.py",
            "preLaunchTask": "installDependencies",
            "internalConsoleOptions": "neverOpen"
        }
    ]
}